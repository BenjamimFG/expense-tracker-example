content:
  application/json:
    schema:
      type: object
      required:
        - name
      properties:
        name:
          type: string
          example: Gas
        description:
          type: string
          example: Expenses related to fuel